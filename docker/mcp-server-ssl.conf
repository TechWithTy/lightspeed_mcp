# Optional MCP Server HTTPS config snippet for production
# Reference this in your FastAPI/Uvicorn setup for SSL/TLS support
# Mount certs to /app/certs and set these paths in your server config or .env

# Example Uvicorn/FastAPI SSL config:
# --ssl-keyfile /app/certs/server.key \
# --ssl-certfile /app/certs/server.crt \
# --ssl-ca-certs /app/certs/ca.crt

# Example .env additions:
# MCP_SERVER_USE_SSL=true
# MCP_SERVER_SSL_KEYFILE=/app/certs/server.key
# MCP_SERVER_SSL_CERTFILE=/app/certs/server.crt
# MCP_SERVER_SSL_CA_CERTS=/app/certs/ca.crt
